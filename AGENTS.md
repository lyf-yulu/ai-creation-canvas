# AI 创作画布协作规则

## 项目定位

本仓库是“AI 创作画布”的独立源码仓库。产品以无限画布承载 AI 图像与视频工作流，前端视觉和画布交互以 Infinite Canvas 为基础，服务端内核优先复用 `ai-generation-portable-apps` 中已经验证成熟的 Portal 设计。

本仓库必须独立维护。运行时不得从 `/Users/260413a/ai-generation-portable-apps` 导入源码、读取状态文件或依赖该目录的路径结构；需要复用的协议或代码应明确迁入本仓库并配套测试。原聚合项目未来只保留薄的 iframe 或反向代理入口。

## 上游与许可证

- Infinite Canvas 上游：`https://github.com/basketikun/infinite-canvas`
- 固定基线提交：`9bccd0ff1a7057a835708a731644ab05371fea3b`
- 上游许可证：AGPL-3.0
- 导入或修改上游代码时，必须保留许可证、版权声明和来源记录。
- 不得在未审阅差异和未验证回归的情况下升级上游基线。
- 新仓库计划公开发布，因此任何提交都必须满足 AGPL-3.0 和第三方资产许可要求。

## 已确认架构

- 前端：固定版本的 Infinite Canvas 源码基础上维护产品 UI、节点与交互。
- 构建：允许在开发和发布阶段使用上游所需的 Node/Bun 工具；生产运行时不得依赖 Node/Bun。
- 运行：静态构建产物由 Python 服务提供。
- 后端：内置 Portal 适配层，复用 Portal 的登录身份、签名代理、资产、任务归属、用量统计和错误处理语义。
- 服务边界：面向“图像生成服务”和“视频生成服务”，不得把 Nano Banana 或 Seedance 写死为一级服务。
- 模型：由服务端模型目录声明可用模型与能力；浏览器不得执行远程模型脚本。
- 人像：属于画布侧视频工作流，顺序为“虚拟人图片上传资产库 → 获得 `asset_id` → 调用视频生成服务”。它不是第三个模型后端。

## 生产隔离

`/Users/260413a/ai-generation-portable-apps` 正在生产运行。除非用户在当前任务中明确授权，否则：

- 不修改生产目录中的任何文件。
- 不重启或停止 `9090`、`8787`、`8797`、`8891` 等生产端口。
- 不操作 launchd 或相关守护进程。
- 不读取 `.env`、密钥文件、Portal `state/`、生成结果、归档、日志或真实请求记录。
- 如需参考原 Portal，只读代码、测试和设计文档；已知可能含硬编码密钥的源码不得打开或输出。

## 端口与测试数据

| 服务 | 生产建议端口 | 测试端口 |
| --- | ---: | ---: |
| AI 创作画布 | 8991 | 8992 |
| Portal | 9090 | 9190 |
| 图像生成代表服务 | 8797 | 8798 |
| 视频生成代表服务 | 8787 | 8788 |
| 人像资产/视频流程 | 8891 | 8892 |

- 自动化和手工验证只能使用测试端口与专用临时目录。
- 测试启动前必须检查配置没有指向生产状态、输出或归档目录。
- 测试结束后验证生产进程与端口未发生变化。

## 安全规则

- 浏览器永远不得保存或获得真实 API Key。
- 用户身份必须来自 Portal 服务端会话或经验证的签名身份；忽略浏览器自报的用户、管理员和任务所有者字段。
- 所有资产、任务、结果和用量操作必须执行用户归属校验。
- 浏览器持久化数据以“环境 + Portal 用户 ID”隔离。
- 普通用户不能配置任意后端 URL、远程插件或动态 JavaScript。
- iframe 和跨域通信使用明确来源白名单，禁止通配来源。
- 上传需要类型、文件头、大小与路径检查；服务端不得抓取用户提供的任意 URL。
- 日志不得记录密钥、完整敏感提示词、上传内容或长期有效的结果地址。

## Git 与文件规则

- `secrets/`、`state/`、`outputs/`、`archives/`、`uploads/`、`logs/`、真实请求记录、证书和密钥不得提交。
- 不提交上游构建缓存、包管理器缓存或本地测试数据。
- 不覆盖历史，不 amend，不 force-push。
- 公开 GitHub 仓库只能在隔离验证通过并获得用户确认后创建。
- 工作树可能含用户改动；不得删除或覆盖无关改动。

## 构建与验证

- 固定依赖版本和上游提交，构建应可复现。
- Python 服务必须能仅凭静态构建产物运行。
- 先测试适配层契约，再连接测试 Portal 和测试生成服务。
- 必须覆盖图像生成、参考图编辑、文本视频、图片视频、人像资产视频五条流程。
- 必须用至少两个测试用户验证登录、浏览器存储、资产、任务和用量隔离。
- 不要求每个模型都做真实付费调用；所有模型做能力契约测试，代表模型做真实隔离冒烟测试。
- 宣称完成或通过前必须运行与声明相匹配的检查并阅读完整结果。

## 设计与实施门禁

- 当前书面设计位于 `docs/superpowers/specs/2026-08-06-ai-creation-canvas-design.md`。
- 用户书面批准设计文档前，不编写实施计划或功能代码。
- 设计获批后先编写可执行的实施计划，再按测试驱动方式实现。
- 设计细节如需改变，先更新设计文档并取得用户确认。
